name: Gitleaks self-scan

on:
  push:
    branches: [develop, main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  gitleaks:
    uses: ./.github/workflows/gitleaks.yml
    with:
      fail_on_finding: false
